# Placeholder — Dockerfile COPY'ит apps/web-buyer/public/, директория должна существовать.
# Реальные ассеты (favicon, og-image) генерируются Next.js из src/app/icon.svg + apple-icon.tsx + opengraph-image.tsx.
