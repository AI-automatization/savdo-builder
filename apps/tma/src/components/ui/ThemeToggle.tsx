/**
 * ThemeToggle: временно скрыт (Polat 07.05). Force-dark до миграции
 * inline `rgba(255,255,255,X)` цветов на CSS-переменные (~553 occurrences
 * в 40 файлах). Когда миграция завершится — вернём 3-кнопочный
 * переключатель Авто/Светлая/Тёмная.
 */
export function ThemeToggle() {
  return null;
}
