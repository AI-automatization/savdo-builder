name: CI — Frontend

on:
  push:
    branches: [main]
    paths:
      - 'apps/tma/**'
      - 'apps/admin/**'
      - 'packages/types/**'
      - 'packages/ui/**'
      - '.github/workflows/ci-frontend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'apps/tma/**'
      - 'apps/admin/**'
      - 'packages/types/**'
      - 'packages/ui/**'

jobs:
  ci:
    name: Type check · Build (${{ matrix.app }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        app: [tma, admin]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        # Версию НЕ указываем: action-setup@v4 берёт её из package.json →
        # packageManager (pnpm@11.12.0) и падает, если задать обе. Единственный
        # источник истины. Под pnpm 9 overrides читались из package.json, а не
        # из pnpm-workspace.yaml — 35 оверрайдов не применялись (issue #10).
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # 22, не 20: pnpm 11 требует Node >=22.13 (issue #10).
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # tsc -b строит packages/types по project references автоматически,
      # затем vite build. Один шаг покрывает type-check + bundle.
      - name: Build
        run: pnpm --filter ${{ matrix.app }} build
