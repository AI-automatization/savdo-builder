{
  "name": "db",
  "version": "0.1.0",
  "private": true,
  "main": "./node_modules/.prisma/client/index.js",
  "types": "./node_modules/.prisma/client/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate resolve --rolled-back 20260509233000_cart_refund_status_enums || true && prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "ts-node --project tsconfig.json prisma/seed.ts",
    "seed:categories": "ts-node --project tsconfig.json prisma/seed-categories.ts",
    "seed:filters": "ts-node --project tsconfig.json prisma/seed-category-filters.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^5.4.0"
  }
}
