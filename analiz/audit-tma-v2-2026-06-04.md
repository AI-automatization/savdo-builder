# TMA E2E Audit v2 — 2026-06-04

> Источник: Ahmed v2 (с Phase 0 verify ролей). Полат phone +998910081910.
> JWT.role = **ADMIN**, seller-row есть (Верифицирован, DRIPSB ЗАБЛОКИРОВАН).
> Гипотеза code-investigation (a44aefaa) **подтверждена** — 8 endpoints дают
> 403 как false-positive из-за role mismatch.

## Стратегия аудита

- BUYER flow тестировался через guest-URL (нормально работает)
- SELLER UI тестировался через TMA Telegram (видна структура, но данные не грузятся — это 403, не баг)
- Seller endpoints возвращают 403 — **корректное поведение** RolesGuard

---

## REAL BUGS (13 шт, не объясняются ролью)

### P0 — Критично

#### BUG-2: Все товары показываются как "Нет в наличии"
- Карточка `/buyer/store/azim-mnx4na25/product/ae330060-...`
- В Admin Panel товар "Игрушка" статус ACTIVE
- TMA показывает красный бейдж "Нет в наличии" + нет кнопки "Добавить в корзину"
- **Воздействие:** блокирует покупки реальных buyer'ов — критично
- **Гипотеза:** `stock`/`inStock` поле не заполнено в БД или маппинг неверный
- **Где смотреть:** `apps/api/src/modules/products/repositories/products.repository.ts` (mapper), `apps/tma/src/pages/buyer/ProductPage.tsx`

### P1 — Серьёзные

#### BUG-1: Счётчик товаров в карточке магазина = 0 (P2-3/4)
- Каталог `/buyer` → Habibulloh и Azim оба показывают `0`
- У Azim 3 активных товара в БД
- **Связано с P2-3/4 (агент a7b64c721 в работе)** — нужно добавить `_count.products` в storefront-stores endpoint
- **Где:** `apps/api/src/modules/storefront/` или `apps/api/src/modules/stores/`

#### BUG-3: Variant отображает hex `#799f` вместо имени
- "Игрушка" → VARIANT: `#799f`
- Должно: читаемое название ("Красный", "Синий")
- **Где:** `apps/tma/src/pages/buyer/ProductPage.tsx` — рендерит `variant.color` вместо `variant.name` или `variant.label`

#### BUG-4: Tab "Магазин" в Seller TMA не работает
- В TMA через Telegram таб "Магазин" не переключает страницу
- Магазин DRIPSB заблокирован → возможно TMA подавляет route без UX-фидбека
- **Где:** `apps/tma/src/pages/seller/` (router + suspended-state UI)

#### BUG-5: Приветствие "Привет," без имени
- Header Seller Dashboard: "Привет, 🔔! 🌙"
- displayName null + нет fallback
- **Где:** `apps/tma/src/pages/seller/DashboardPage.tsx`

#### BUG-6: Аналитика не грузится (Seller Dashboard)
- "Не удалось загрузить аналитику"
- **Гибридная гипотеза**: либо 403 от ADMIN роли (false-positive), либо реальный баг endpoint
- Требует тест с реальным SELLER аккаунтом для подтверждения

#### BUG-7: Счётчики "Товаров" и "Всего заказов" = `—`
- Связано с BUG-6 — 403 от endpoints
- **Решение:** не баг (false-positive после Phase 0 verification)

### P2 — UX

#### BUG-8: Splash screen "Savdo" + фиолетовый
- При запуске TMA через Telegram: сплэш фиолетовая иконка + "Savdo"
- Должно: maxsavdo + Champagne Gold
- **Где:** `apps/tma/src/index.html`, `apps/tma/public/manifest.json`, `apps/tma/src/App.tsx` splash

#### BUG-9: Header H1 каталога "Savdo" вместо "maxsavdo"
- Header buyer каталога текст "Savdo"
- В sidebar логотип "maxsavdo" корректный
- **Где:** `apps/tma/src/pages/buyer/StoresPage.tsx` (header text) или layout-компонент

#### BUG-10: Разные тексты "нет фото"
- Карточка "Игрушка": "НЕТ ФОТО" (camera icon)
- "Белая футболка", "Наушники": "ФОТО НЕДОСТУПНО" (warning icon)
- **Где:** `apps/tma/src/components/ImagePlaceholder.tsx`

#### BUG-11: Seller TMA застревает на "Заказы"
- Переход на "Заказы" ломает все остальные табы
- Требуется закрыть/открыть TMA
- **Где:** state machine navigation, возможно `apps/tma/src/providers/`

#### BUG-12: UZ перевод не полный — sidebar остаётся на русском
- "Настройки" не переведён в UZ-mode

#### BUG-13: Кнопка "Добавить в корзину" исчезает при OOS
- Связано с BUG-2
- В списке магазина кнопка `+` работает (несоответствие)

---

## DESIGN STATUS (Phase 5)

| Критерий | Статус |
|----------|--------|
| Champagne Gold #C9A876 | ✅ ВЕРНО (rgb 201,168,118 обнаружен) |
| Inter font | ✅ ВЕРНО (font-family первый) |
| maxsavdo в sidebar | ✅ ВЕРНО |
| maxsavdo в header | ❌ "Savdo" (BUG-9) |
| Splash screen | ❌ фиолет + "Savdo" (BUG-8) |
| Orchid Violet #A855F7 | ✅ удалён из buyer |
| Тема синх с Telegram | ✅ data-theme="light" работает |
| Browser title | ❌ "Savdo" |
| Иконка избранного | ❌ фиолетовая (heart icon) |

---

## FALSE-POSITIVE (8 шт, объясняется ADMIN ролью)

| Endpoint | Status | Причина |
|----------|--------|---------|
| `/seller/products` | 403 | RolesGuard ADMIN ≠ SELLER |
| `/seller/orders` | 403 | то же |
| `/seller/analytics/*` | 403 | то же |
| `/seller/store/*` | 403 | то же |
| `/chat/*` | 403 | @Roles('BUYER','SELLER') блокирует ADMIN |
| Dashboard counters | `—` | от 403 endpoints |

**НЕ помечать как баги** — корректное поведение API.

---

## NETWORK FINDINGS

Buyer-side всё 200:
- `/storefront/stores` 200
- `/storefront/stores/azim-mnx4na25` 200
- `/stores/azim-mnx4na25/products` 200
- `/storefront/categories` 200
- `/analytics/track` 204

`/seller/dashboard/counters` — отдельного endpoint нет (интегрирован в dashboard).

---

## ПОЗИТИВНОЕ

- Skeleton Loading ✅
- Поиск магазинов мгновенный ✅
- Корзина (+/-/удалить/subtotal) ✅
- Checkout форма + auth-gate для guest ✅
- Фильтры категорий ✅
- Локализация RU/UZ (кроме sidebar) ✅
- Тема авто-синх с Telegram ✅
- Admin Panel — полнофункциональна ✅
- Inter шрифт загружается (8 woff2) ✅
- Champagne Gold в buyer ✅
- Защита guest-mode (UX-сообщения) ✅

---

## РЕКОМЕНДАЦИИ

1. **Срочно BUG-2 (P0)** — починить отображение stock/inStock в TMA
2. **BUG-1** — связан с P2-3/4 (агент в работе) — добавить `_count.products` в storefront endpoint
3. **BUG-3** — `variant.name` рендер вместо `variant.color`
4. **BUG-8, BUG-9** — final-touch миграции TMA-DESIGN-V2 (агент `a60439d5` это решает)
5. Для полного seller-flow аудита — нужен **отдельный SELLER аккаунт** (Полат — ADMIN)

---

*Связано: `audit-2026-06-04.md` (admin часть, первая версия аудита),
`audit-tma-2026-06-04.md` (первая TMA версия — устаревшая),
`logs.md` (детальные баги).*
